# AUTORELEASE v0.2.4.dev0
# for nonrelease, use @master
# for release, use v${VERSION}, e.g., v1.0.0
stages:
    - test
    - deploy testpypi
    - test testpypi
    - cut release
    - deploy pypi

import:
    - dwhswenson/autorelease:travis_stages/deploy_testpypi.yml@travis-deploy
    - dwhswenson/autorelease:travis_stages/test_testpypi.yml@travis-deploy
    - dwhswenson/autorelease:travis_stages/cut_release.yml@travis-deploy
    - dwhswenson/autorelease:travis_stages/deploy_pypi.yml@travis-deploy
